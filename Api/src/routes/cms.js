import express from 'express';
// import users from "../domains/users/CMSRoute";
// import roles from "../domains/roles/CMSRoute";

const router = express.Router();

// router.use('/users', users);
// router.use('/roles', roles);

export default router;
